// What will the following two lines of code output?

console.log(0.3 + 0.6); // some floaring point number apporoximation
console.log(0.3 + 0.6 === 0.9); // false

// JS uses floating point numbers for all numeric operations. Because of this,
// the numbers lack precision and may yield unexpeted results.
